# TODO: Python script to start a generic server and parse out the following uris:
# misc (should be output as a string)
# miscb64 (should parse out the base64 and output as a string)
# cookie (should parse out the cookie and output as a string)
# cb64 (should parse out the base64 and output as a string)